支持全局http header和局部单个apiheader
支持全局变量 和 案例变量
支持api返回值，返回值可在后续api中使用
支持打tag标签，执行可过滤和筛选，支持和TP案例ID联动
支持baisc授权，可支持全局授权和局部单个api授权
支持执行外部命令，方便扩展框架执行能力
支持跳过单个api步骤，调试使用
支持执行单个api步骤
支持多状态码校验，清理步骤使用
支持http和https协议，可在env.yml文件中定义
api框架实际上是pytest插件，支持pytest自定义扩展，也可使用pytest丰富的其他插件扩展